class Tools:
    def search_web(self, query):
        # 範例：這裡可以接 API
        return f"搜尋結果: {query}"

    def send_email(self, to, subject, body):
        # 範例：這裡可以接 SMTP 或 API
        return f"寄信給 {to}, 主題: {subject}, 內容: {body}"
